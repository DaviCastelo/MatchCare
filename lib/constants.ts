// Soft weekly-hours target for therapists. This is a minimum the clinic wants
// each therapist to reach — it never blocks matching, it only drives warnings
// and the load bonus in the matching engine.
export const THERAPIST_WEEKLY_TARGET = 15
